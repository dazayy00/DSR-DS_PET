/*
Map<String, List<String>> getNameAndIdListsPet(List<dynamic> lista) {
  List<String> nameList = [];
  List<String> idList = [];

  for (var elemento in lista) {
    nameList.add(elemento['name']);
    idList.add(elemento['id']);
  }

  return {
    'names': nameList,
    'ids': idList
  };
}
*/